# Flat mirror of pyproject's core dependencies, for `pip install -r requirements.txt`
# (e.g. on Kaggle where editable installs are awkward). Prefer `pip install -e .`
# locally so the console scripts and package resolve properly.
#
# This list is exactly what the code imports. albumentations, opencv-python-headless,
# scikit-image and torch-uncertainty used to be pinned here too but are imported
# nowhere in the project -- see the note in pyproject.toml. torch-uncertainty is
# now an opt-in extra: `pip install -e ".[uq]"`.

torch>=2.1
torchvision>=0.16
timm>=1.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.66
pillow>=10.0

# Optional: only needed to pull raw datasets through the Kaggle CLI.
# kaggle>=1.6
