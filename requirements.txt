# Core dependencies for xctb.
#
# On Kaggle, torch / torchvision / timm / numpy / pandas / scikit-learn / pillow
# are already installed, so you usually only need: pip install pyyaml torchmetrics
#
# Version floors below are conservative. They were exercised against the current
# PyPI releases (torch 2.x, numpy 2.x, pandas 2.x/3.x). Pin harder if you need
# bit-for-bit reproducibility for the paper.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
pillow>=10.0
tqdm>=4.60

# deep learning (skip on Kaggle; install a CUDA build that matches your machine)
torch>=2.1
torchvision>=0.16
timm>=1.0

# reading RSNA DICOMs (only needed if you use the RSNA cohort)
pydicom>=2.4
